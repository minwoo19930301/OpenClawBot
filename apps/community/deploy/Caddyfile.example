# Replace with a domain whose DNS already points to the chosen server.
# Match COMMUNITY_ORIGIN to https://that-domain.
your-domain.example {
    encode zstd gzip
    reverse_proxy 127.0.0.1:8787
    header {
        X-Content-Type-Options nosniff
        Referrer-Policy no-referrer
    }
}
